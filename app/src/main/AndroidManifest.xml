<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chewbs.alloheroproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".authentication.UserSigninActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authentication.UserSignupActivity" />
        <activity android:name=".authentication.AssociationSigninActivity" />
        <activity android:name=".authentication.AssociationSignupActivity" />
        <activity android:name=".association.AssociationHomeActivity" />
        <activity android:name=".user.UserHomeActivity" />
        <activity android:name=".user.UserAssociationListActivity" />
        <activity android:name=".association.AssociationSelectedLocationActivity" />
        <activity android:name=".association.AssociationSelectedUserActivity" />
        <activity android:name=".user.UserSelectedAssociationActivity" />
        <activity android:name=".association.AssociationUserListActivity"></activity>
    </application>

</manifest>